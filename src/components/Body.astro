---
interface Props {
  as?: 'p' | 'div' | 'span';
  class?: string;
}

const { as: Tag = 'p', class: className } = Astro.props;
---

<Tag class:list={['text-body', 'text-text-primary', className]}>
  <slot />
</Tag>
